package ${package}.${moduleName}.controller.api;

import ${package}.entity.model.${className};
import ${package}.service.util.PageResult;
import ${package}.service.util.Result;
import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;

import java.util.Map;
/**
 * @Author: ${author}
 * @Description: ${comments}接口
 * @Date: ${datetime}
 */
@Api(tags = "${comments}")
@RequestMapping("/${classname}")
@Validated
public interface I${className}Resource {
    /**
     * 分页条件查询
     * @param params 需要包含 pageNum,pageSize
     * @return PageInfo
     * @throws Exception
     */
    @ApiOperation(value = "查询列表")
    @PostMapping("/list")
    Result<PageResult<${className}>> findList(@RequestBody Map<String, Object> params) throws Exception;
}
